ALTER TABLE materias ADD Eliminado TINYINT(4) NOT NULL;

ALTER TABLE asignaturas ADD Eliminado TINYINT(4) NOT NULL;

CREATE TABLE IF NOT EXISTS Docentes(idDocente INTEGER PRIMARY KEY AUTOINCREMENT, Documento TEXT NOT NULL UNIQUE, Nombre TEXT NOT NULL, Apellido TEXT NOT NULL, FNac TEXT, Edad INTEGER, Grado INTEGER, FechaIngreso TEXT, Eliminado TINYINT(4));

ALTER TABLE Cursos ADD Docente INTEGER NOT NULL REFERENCES Docentes(idDocente);

ALTER TABLE Cursos ADD COLUMN Puntaje INTEGER
ALTER TABLE Cursos ADD COLUMN Edicion INTEGER
ALTER TABLE Cursos ADD COLUMN anno INTEGER;
ALTER TABLE Cursos ADD COLUMN Eliminado TINYINT(4);
ALTER TABLE Cursos ADD COLUMN Estado TEXT;

ALTER TABLE Estudiantes ADD COLUMN Eliminado TINYINT(4);

CREATE TABLE IF NOT EXISTS Examenes(idExamen INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, ExamenesCurso INTEGER, Fecha DATE, Eliminado TINYINT(4), FOREIGN KEY(ExamenesCurso) REFERENCES Cursos(idCurso));

CREATE TABLE IF NOT EXISTS RendicionesExamenes(idRendicionesExamenes INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, Examen INTEGER, Inscripcion INTEGER, Nota INTEGER,Eliminado TINYINT(4), FOREIGN KEY(Examen) REFERENCES Examenes(idExamen), FOREIGN KEY(Inscripcion) REFERENCES InscripcionesCurso(idInscripcionesCurso));